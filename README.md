# tea

Static clone of the MOUK VSL landing page.

## Local preview

```bash
cd tea
python3 -m http.server 8080
```

Open http://localhost:8080

## Notes

- Video player loads from ConverteAI CDN (requires network).
- Facebook Pixel and external tracking scripts remain as in the source page.
